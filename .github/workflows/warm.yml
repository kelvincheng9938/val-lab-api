name: Warm API hourly
on:
  schedule:
    - cron: "0 * * * *"   # 每小時，改成 */15 * * * * = 每15分鐘
  workflow_dispatch:       # 允許手動觸發

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping warm endpoint
        run: |
          curl -fsS https://val-lab-api.vercel.app/api/warm || true
